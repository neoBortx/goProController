package com.bortxapps.goprocontrollerandroid.domain.data

enum class GoProBleConnectionStatus {
    STATE_CONNECTED,
    STATE_DISCONNECTED,
    STATE_CONNECTING,
    STATE_DISCONNECTING,
    UNKNOWN
}